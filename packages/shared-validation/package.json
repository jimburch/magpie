{
	"name": "@coati/validation",
	"version": "0.1.0",
	"description": "Shared validation constants, types, regex patterns, and Zod schemas for Coati enums",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "vitest run",
		"test:unit": "vitest run",
		"typecheck": "tsc --noEmit"
	},
	"peerDependencies": {
		"zod": ">=3.0.0"
	},
	"devDependencies": {
		"typescript": "^5.9.3",
		"vitest": "^2.1.9"
	}
}
